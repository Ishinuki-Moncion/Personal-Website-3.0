.DS_Store
.thumbnail

# daikieOS v3 visual-study board — raw third-party reference frames (provenance lives in committed study entries)
docs/superpowers/research/refs/

# Personal Claude Code overrides (local, not shared)
.claude/settings.local.json

# v3.2 gate screenshots (owner end-of-pass review artifacts; heavy PNGs, not site assets)
docs/superpowers/gates/v32/

# v3.3 gate screenshots (same class: owner review artifacts, git-ignored — spec §4)
docs/superpowers/gates/v33/

# Codex CLI local environment config (owner tool state, not site content)
.codex/

# Isolated implementation worktrees created for audited agent branches
.worktrees/

# Local Node and Playwright output
node_modules/
test-results/
playwright-report/

# Codex v3.4 corrective-completion QA evidence (immutable local gate captures)
.superpowers/gates/codex-v34/

# Upstream OFL source faces for tools/build-fonts.mjs (~11MB of TTF, refetchable
# with `npm run fonts:fetch`). Only the generated fonts/*.woff2 subsets and the
# OFL license files are committed.
.fontsrc/

# Generated Pages deploy artifact (tools/build-artifact.mjs)
dist/
