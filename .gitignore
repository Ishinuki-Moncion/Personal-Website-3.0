.DS_Store
.thumbnail

# daikieOS v3 visual-study board — raw third-party reference frames (provenance lives in committed study entries)
docs/superpowers/research/refs/

# Personal Claude Code overrides (local, not shared)
.claude/settings.local.json
